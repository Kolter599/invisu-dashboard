'use client';

export default function Page() {
  return (
    <div style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>✅ /trefadder/dashboard is working</h1>
      <p>If you see this, the route exists in the deployed build.</p>
      <a href="/">Back to home</a>
    </div>
  );
}
